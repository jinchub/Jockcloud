const registerAllRoutes = (app, deps) => {
  const {
    authRequired,
    adminRequired,
    pool,
    loadUserGroupContextMap,
    getEffectivePermissions,
    parsePermissionList,
    sendDbError,
    normalizeIdList,
    insertUserGroupMembers,
    hashPassword,
    verifyPassword,
    resolveAbsoluteStoragePath,
    fs,
    normalizeUserGroupUploadMaxSizeMb,
    normalizeUserGroupUploadMaxFileCount,
    convertUserGroupUploadSizeMbToGb,
    convertUserGroupUploadSizeGbToMb,
    cosUploadSingle,
    parseMountConfig,
    ensureObjectMount,
    getMountById,
    normalizeObjectKey,
    createCosClientByMount,
    createQiniuClientByMount,
    createOssClientByMount,
    cosRequest,
    qiniuBucketRequest,
    qiniuUploadRequest,
    qiniu,
    path,
    fetch,
    Buffer,
    encodeCosKey,
    readSettings,
    verifyCaptcha,
    createLoginSession,
    generateCaptcha,
    normalizePhone,
    isSmsConfigComplete,
    getSmsPolicyConfig,
    smsCodeStore,
    smsIpRateStore,
    SMS_CODE_EXPIRE_MS,
    dispatchSmsCode,
    logInfo,
    logError,
    verifySmsCode,
    SESSION_COOKIE,
    decryptLoginPassword,
    LOGIN_PASSWORD_KEY_ID,
    LOGIN_PASSWORD_KEY_PAIR,
    requireFilePermission,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    checkFolderOwnership,
    hasNameConflict,
    hasEntryNameConflict,
    hasFilePermission,
    collectDescendantFolderIds,
    toInClause,
    chunkUploadSingle,
    uploadArray,
    normalizeChunkClientTaskId,
    normalizeRelativePath,
    safeFileName,
    normalizeUploadName,
    MAX_UPLOAD_CHUNK_SIZE_BYTES,
    getUploadRuntimeOptions,
    DEFAULT_SETTINGS,
    getUploadCategoryRuntimeOptions,
    getUploadLimitError,
    getUploadFormatError,
    findChunkSessionByFile,
    getChunkMarksDir,
    removeChunkSessionsByClientTaskId,
    crypto,
    getChunkSessionDir,
    getChunkDataPath,
    writeChunkMeta,
    removeChunkSession,
    normalizeChunkUploadId,
    readChunkMeta,
    normalizeStorageSpaceType,
    getUploadStorageDir,
    resolveStorageRootDir,
    resolveStorageNameFromPath,
    resolveUploadCategory,
    writeThumbnailFromDataUrl,
    normalizeFileCategoryKey,
    resolveStoredFileCategory,
    resolveFolderByRelativePath,
    resolveUniqueName,
    logFileOperation,
    detectArchiveType,
    ARCHIVE_SUPPORTED_TYPE_SET,
    listArchiveEntries,
    resolveFolderByRelativeDir,
    os,
    extractArchiveToDirectory,
    inferMimeTypeByFileName,
    inferImageMimeTypeFromStorageName,
    mammoth,
    xlsx,
    writeExtractedThumbnailFromSource,
    runCompressArchive,
    Throttle,
    getUserDownloadSpeedLimit,
    createSpeedLimitedStream,
    resolveTransferTaskTypeByRequest,
    getTransferTaskText,
    normalizeUploadTaskItem,
    normalizeUploadTaskStatus,
    normalizeSyncDirection,
    normalizeSyncTaskType,
    normalizeSyncScheduleUnit,
    normalizeSyncScheduleTime,
    normalizeSyncScheduleAt,
    normalizeSyncScheduleDateType,
    normalizeSyncScheduleDateValue,
    normalizeSyncEmptyDirMode,
    normalizeSyncFileUpdateRule,
    normalizeSyncDeleteRule,
    normalizeSyncTaskStatus,
    normalizeSyncTaskItem,
    getSyncTaskNextRunAt,
    formatSyncDetailTime,
    appendSyncTaskHistoryLog,
    runSyncTaskNow,
    mergeSettingsPayload,
    normalizeSettings,
    writeSettings,
    setCurrentMaxUploadFileSizeByMb,
    getAllowedMenusForUser,
    getMobileVisibleMenus,
    MENU_PERMISSION_KEYS,
    normalizeViewMode,
    normalizeGridSize,
    normalizeVisibleCategories,
    resolveGroupUploadMaxSizeMb,
    resolveGroupUploadMaxFileCount,
    avatarUploadSingle,
    UPLOAD_DIR,
    getAvatarStorageDir,
    normalizeStorageRelativePath,
    apiMonitorStore,
    PREVIEW_MEDIA_STREAM_CHUNK_BYTES,
    copyFileRecord,
    copyFolderRecursive,
    cleanupExpiredRecycleEntries,
    buildFolderLogicalPathResolver
  } = deps;

  const registerUserRoutes = require("./users");
  const routeDeps = {
    authRequired,
    adminRequired,
    pool,
    loadUserGroupContextMap,
    getEffectivePermissions,
    parsePermissionList,
    sendDbError,
    normalizeIdList,
    insertUserGroupMembers,
    hashPassword,
    verifyPassword,
    resolveAbsoluteStoragePath,
    fs,
    normalizeUserGroupUploadMaxSizeMb,
    normalizeUserGroupUploadMaxFileCount,
    convertUserGroupUploadSizeMbToGb,
    convertUserGroupUploadSizeGbToMb
  };
  registerUserRoutes(app, routeDeps);

  const registerMountRoutes = require("./mounts");
  registerMountRoutes(app, {
    authRequired,
    cosUploadSingle,
    pool,
    sendDbError,
    parseMountConfig,
    ensureObjectMount,
    getMountById,
    normalizeObjectKey,
    createCosClientByMount,
    createQiniuClientByMount,
    createOssClientByMount,
    cosRequest,
    qiniuBucketRequest,
    qiniuUploadRequest,
    qiniu,
    path,
    fetch,
    Buffer,
    encodeCosKey
  });

  const registerAuthRoutes = require("./auth");
  registerAuthRoutes(app, {
    authRequired,
    pool,
    readSettings,
    verifyCaptcha,
    hashPassword,
    verifyPassword,
    createLoginSession,
    sendDbError,
    generateCaptcha,
    normalizePhone,
    isSmsConfigComplete,
    getSmsPolicyConfig,
    smsCodeStore,
    smsIpRateStore,
    SMS_CODE_EXPIRE_MS,
    dispatchSmsCode,
    logInfo,
    logError,
    verifySmsCode,
    SESSION_COOKIE,
    decryptLoginPassword,
    LOGIN_PASSWORD_KEY_ID,
    LOGIN_PASSWORD_KEY_PAIR
  });

  const registerStorageMetaRoutes = require("./storage-meta");
  registerStorageMetaRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    checkFolderOwnership,
    hasNameConflict,
    hasEntryNameConflict,
    hasFilePermission,
    collectDescendantFolderIds,
    toInClause
  });

  const registerUploadBasicRoutes = require("./uploads-basic");
  registerUploadBasicRoutes(app, {
    authRequired,
    requireFilePermission,
    chunkUploadSingle,
    uploadArray,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeChunkClientTaskId,
    normalizeFolderId,
    normalizeRelativePath,
    safeFileName,
    normalizeUploadName,
    MAX_UPLOAD_CHUNK_SIZE_BYTES,
    checkFolderOwnership,
    getUploadRuntimeOptions,
    DEFAULT_SETTINGS,
    getUploadCategoryRuntimeOptions,
    readSettings,
    getUploadLimitError,
    getUploadFormatError,
    findChunkSessionByFile,
    getChunkMarksDir,
    fs,
    removeChunkSessionsByClientTaskId,
    crypto,
    getChunkSessionDir,
    getChunkDataPath,
    writeChunkMeta,
    removeChunkSession,
    normalizeChunkUploadId,
    readChunkMeta,
    normalizeStorageSpaceType,
    path,
    getUploadStorageDir,
    resolveStorageRootDir,
    resolveStorageNameFromPath,
    resolveUploadCategory,
    writeThumbnailFromDataUrl,
    normalizeFileCategoryKey,
    resolveStoredFileCategory,
    resolveFolderByRelativePath,
    hasEntryNameConflict,
    hasFilePermission,
    resolveUniqueName,
    logFileOperation
  });

  const registerDownloadRoutes = require("./downloads");
  registerDownloadRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    detectArchiveType,
    ARCHIVE_SUPPORTED_TYPE_SET,
    resolveAbsoluteStoragePath,
    fs,
    listArchiveEntries,
    normalizeRelativePath,
    checkFolderOwnership,
    resolveFolderByRelativeDir,
    path,
    os,
    extractArchiveToDirectory,
    getUploadCategoryRuntimeOptions,
    DEFAULT_SETTINGS,
    readSettings,
    getUploadStorageDir,
    resolveStorageRootDir,
    crypto,
    resolveStorageNameFromPath,
    inferMimeTypeByFileName,
    normalizeFileCategoryKey,
    mammoth,
    xlsx,
    Buffer,
    resolveStoredFileCategory,
    logFileOperation,
    writeExtractedThumbnailFromSource,
    runCompressArchive,
    resolveUniqueName,
    collectDescendantFolderIds,
    safeFileName,
    hasFilePermission
  });

  const registerShareRoutes = require("./shares");
  registerShareRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    hasFilePermission,
    normalizeStorageSpaceType,
    collectDescendantFolderIds,
    hashPassword,
    verifyPassword,
    readSettings,
    resolveAbsoluteStoragePath,
    fs,
    path,
    os,
    runCompressArchive,
    safeFileName,
    crypto,
    Throttle,
    getUserDownloadSpeedLimit,
    createSpeedLimitedStream,
    logFileOperation,
    normalizeFileCategoryKey,
    resolveStoredFileCategory,
    getUploadCategoryRuntimeOptions,
    DEFAULT_SETTINGS
  });

  const registerHiddenSpaceRoutes = require("./hidden-space");
  registerHiddenSpaceRoutes(app, {
    authRequired,
    pool,
    sendDbError,
    hashPassword,
    verifyPassword,
    readSettings,
    isSmsConfigComplete,
    normalizePhone,
    getSmsPolicyConfig,
    smsCodeStore,
    smsIpRateStore,
    SMS_CODE_EXPIRE_MS,
    dispatchSmsCode,
    logInfo,
    logError,
    verifySmsCode
  });

  const registerUploadTasksRoutes = require("./upload-tasks");
  registerUploadTasksRoutes(app, {
    authRequired,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    resolveTransferTaskTypeByRequest,
    getTransferTaskText,
    normalizeUploadTaskItem,
    normalizeUploadTaskStatus
  });

  const registerSyncTasksRoutes = require("./sync-tasks");
  registerSyncTasksRoutes(app, {
    authRequired,
    pool,
    sendDbError,
    normalizeSyncDirection,
    normalizeSyncTaskType,
    normalizeSyncScheduleUnit,
    normalizeSyncScheduleTime,
    normalizeSyncScheduleAt,
    normalizeSyncScheduleDateType,
    normalizeSyncScheduleDateValue,
    normalizeSyncEmptyDirMode,
    normalizeSyncFileUpdateRule,
    normalizeSyncDeleteRule,
    normalizeSyncTaskStatus,
    normalizeSyncTaskItem,
    getSyncTaskNextRunAt,
    formatSyncDetailTime,
    appendSyncTaskHistoryLog,
    runSyncTaskNow
  });

  const registerSyncTasksStreamRoutes = require("./sync-tasks-stream");
  registerSyncTasksStreamRoutes(app, {
    authRequired,
    pool,
    getRuntimeDeps: () => ({
      normalizeSyncTaskType,
      normalizeSyncScheduleUnit,
      normalizeSyncScheduleTime,
      normalizeSyncScheduleAt,
      normalizeSyncScheduleDateType,
      normalizeSyncScheduleDateValue,
      getSyncTaskNextRunAt,
      formatSyncDetailTime,
      appendSyncTaskHistoryLog,
      normalizeSyncTaskStatus,
      runSyncTaskNow
    })
  });

  const registerSettingsRoutes = require("./settings");
  registerSettingsRoutes(app, {
    authRequired,
    adminRequired,
    sendDbError,
    readSettings,
    DEFAULT_SETTINGS,
    isSmsConfigComplete,
    mergeSettingsPayload,
    normalizeSettings,
    writeSettings,
    setCurrentMaxUploadFileSizeByMb
  });

  const registerProfileRoutes = require("./profile");
  registerProfileRoutes(app, {
    authRequired,
    pool,
    sendDbError,
    loadUserGroupContextMap,
    getEffectivePermissions,
    parsePermissionList,
    resolveGroupUploadMaxSizeMb,
    resolveGroupUploadMaxFileCount,
    readSettings,
    getAllowedMenusForUser,
    getMobileVisibleMenus,
    MENU_PERMISSION_KEYS,
    normalizeViewMode,
    normalizeGridSize,
    normalizeVisibleCategories,
    normalizeUserGroupUploadMaxSizeMb,
    normalizeUserGroupUploadMaxFileCount,
    normalizePhone,
    hashPassword,
    avatarUploadSingle,
    path,
    UPLOAD_DIR,
    crypto,
    fs,
    getAvatarStorageDir,
    normalizeStorageRelativePath
  });

  const registerAdminStatsRoutes = require("./admin-stats");
  registerAdminStatsRoutes(app, {
    authRequired,
    adminRequired,
    pool,
    sendDbError
  });

  app.locals.pool = pool;
  app.locals.apiMonitorStore = apiMonitorStore;

  const registerSystemMonitorRoutes = require("./system-monitor");
  registerSystemMonitorRoutes(app, {
    authRequired,
    adminRequired,
    apiMonitorStore,
    pool
  });

  const registerOnlineUsersRoutes = require("./online-users");
  registerOnlineUsersRoutes(app, {
    authRequired,
    adminRequired,
    pool,
    sendDbError
  });

  const registerPreviewRoutes = require("./preview");
  registerPreviewRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    resolveAbsoluteStoragePath,
    inferImageMimeTypeFromStorageName,
    fs,
    hasFilePermission,
    Buffer,
    mammoth,
    xlsx,
    PREVIEW_MEDIA_STREAM_CHUNK_BYTES
  });

  const registerEntriesRoutes = require("./entries");
  registerEntriesRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    normalizeFileCategoryKey,
    resolveStoredFileCategory,
    collectDescendantFolderIds,
    toInClause,
    hasFilePermission,
    checkFolderOwnership,
    hasNameConflict,
    copyFileRecord,
    copyFolderRecursive,
    resolveUniqueName,
    safeFileName,
    normalizeStorageSpaceType
  });

  const registerRecycleRoutes = require("./recycle");
  registerRecycleRoutes(app, {
    authRequired,
    requireFilePermission,
    pool,
    sendDbError,
    resolveStorageSpaceTypeByRequest,
    normalizeFolderId,
    normalizeFileCategoryKey,
    resolveStoredFileCategory,
    checkFolderOwnership,
    collectDescendantFolderIds,
    toInClause,
    resolveAbsoluteStoragePath,
    fs,
    cleanupExpiredRecycleEntries,
    buildFolderLogicalPathResolver
  });
};

module.exports = {
  registerAllRoutes
};
